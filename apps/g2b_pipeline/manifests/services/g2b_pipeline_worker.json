{
  "service_name": "g2b-pipeline-worker",
  "service_type": "consumer",
  "dockerfile": "apps/g2b_pipeline/infra/image/Dockerfile",
  "build_context": "../..",
  "user": "${HOST_UID:-1000}:${HOST_GID:-1000}",
  "restart": "unless-stopped",
  "env_files": [
    "apps/g2b_pipeline/infra/env/app.env"
  ],
  "command": [
    "sh",
    "-c",
    "prefect work-pool inspect $$PREFECT_WORK_POOL >/dev/null 2>&1 || prefect work-pool create $$PREFECT_WORK_POOL --type process; python -m apps.g2b_pipeline.app.deployments; prefect worker start --pool $$PREFECT_WORK_POOL"
  ],
  "depends_on_service_healthy": [
    "prefect-server"
  ]
}
