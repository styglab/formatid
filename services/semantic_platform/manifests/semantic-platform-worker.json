{
  "service_name": "semantic-platform-worker",
  "service_type": "consumer",
  "dockerfile": "services/semantic_platform/adapters/worker/infra/image/Dockerfile",
  "build_context": "../..",
  "user": "${HOST_UID:-1000}:${HOST_GID:-1000}",
  "restart": "unless-stopped",
  "env_files": [
    "services/semantic_platform/adapters/worker/infra/env/app.env"
  ],
  "volumes": [
    "sources:/app/sources"
  ],
  "command": [
    "sh",
    "-c",
    "prefect work-pool inspect $$PREFECT_WORK_POOL >/dev/null 2>&1 || prefect work-pool create $$PREFECT_WORK_POOL --type process; python -m services.semantic_platform.adapters.worker.deployments; prefect worker start --pool $$PREFECT_WORK_POOL"
  ],
  "depends_on_service_healthy": [
    "embedding-service",
    "prefect-server"
  ]
}
