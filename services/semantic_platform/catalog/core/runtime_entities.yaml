entities:

  CalendarEvent:
    description_ko: "CalendarEvent runtime entity inferred from public API source semantics."
    semantic_types: [event_date, event_name]
    capabilities: [get_special_day]
    identifiers: []

  BusinessRegistration:
    description_ko: "BusinessRegistration runtime entity inferred from public API source semantics."
    semantic_types: [business_open_date, business_status, tax_type, valid_status]
    capabilities: [check_business_status, validate_business_registration]
    identifiers: []

  Document:
    description_ko: "Document runtime entity inferred from public API source semantics."
    semantic_types: [result_message]
    capabilities: []
    identifiers: []

  Location:
    description_ko: "Location runtime entity inferred from public API source semantics."
    semantic_types: [business_address]
    capabilities: []
    identifiers: []

  Organization:
    description_ko: "Organization runtime entity inferred from public API source semantics."
    semantic_types: [business_registration_number, corporation_number, organization_name]
    capabilities: [check_business_status, validate_business_registration]
    identifiers: [business_registration_number, corporation_number]

  Person:
    description_ko: "Person runtime entity inferred from public API source semantics."
    semantic_types: [representative_name]
    capabilities: [validate_business_registration]
    identifiers: []

  Parcel:
    description_ko: "Parcel runtime entity inferred from public API source semantics."
    semantic_types: [land_price, lot_number, pnu]
    capabilities: [get_cadastral_features, get_cadastral_schema]
    identifiers: [pnu]

  Measurement:
    description_ko: "Measurement runtime entity inferred from public API source semantics."
    semantic_types: []
    capabilities: [get_asos_hourly_observation]
    identifiers: []

  WeatherObservation:
    description_ko: "WeatherObservation runtime entity inferred from public API source semantics."
    semantic_types: []
    capabilities: [get_asos_hourly_observation]
    identifiers: []

  FinancialStatement:
    description_ko: "FinancialStatement runtime entity inferred from public API source semantics."
    semantic_types: [financial_revenue]
    capabilities: [get_financial_summary, get_income_statement]
    identifiers: []

  Contract:
    description_ko: "Contract runtime entity inferred from public API source semantics."
    semantic_types: [confirmed_contract_number, contract_amount, contract_date, contract_id, contract_name, contract_reference_number, procurement_category]
    capabilities: [get_procurement_contract_detail, search_procurement_contract_change_history, search_procurement_contract_delete_history, search_procurement_contracts, search_procurement_contracts_by_conditions]
    identifiers: [contract_id]

  Event:
    description_ko: "Event runtime entity inferred from public API source semantics."
    semantic_types: [registration_datetime]
    capabilities: []
    identifiers: []

  AirQualityObservation:
    description_ko: "AirQualityObservation runtime entity inferred from public API source semantics."
    semantic_types: [air_quality_grade, air_quality_index, main_pollutant, pm10_value]
    capabilities: [get_air_quality_realtime]
    identifiers: []
