# mcp

MCP export and runtime adapters for semantic platform capabilities.

Current MCP integration is implemented in `apps/pubdata_mcp`, which calls the
semantic platform API. This directory is reserved for generated MCP schemas,
tool manifests, and adapters when tool generation moves into this app.
