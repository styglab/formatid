import { ContextLayout } from "@/components/layout/context-layout";

export default function ContextRouteLayout({ children }: { children: React.ReactNode }) {
  return <ContextLayout>{children}</ContextLayout>;
}
