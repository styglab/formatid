{
  "service_name": "redis",
  "image": "redis:7-alpine",
  "ports": [
    "${HOST_BIND_IP:-127.0.0.1}:${REDIS_HOST_PORT:-6380}:${REDIS_CONTAINER_PORT:-6379}"
  ],
  "volumes": [
    "services/redis/redis.conf:/usr/local/etc/redis/redis.conf:ro"
  ],
  "healthcheck": {
    "test": [
      "CMD",
      "redis-cli",
      "ping"
    ],
    "interval": "2s",
    "timeout": "2s",
    "retries": 10
  }
}
